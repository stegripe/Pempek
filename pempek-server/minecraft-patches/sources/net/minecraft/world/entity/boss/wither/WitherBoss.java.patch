--- a/net/minecraft/world/entity/boss/wither/WitherBoss.java
+++ b/net/minecraft/world/entity/boss/wither/WitherBoss.java
@@ -81,6 +_,7 @@
     private static final TargetingConditions.Selector LIVING_ENTITY_SELECTOR = (target, level) -> !target.is(EntityTypeTags.WITHER_FRIENDS)
         && target.attackable();
     private static final TargetingConditions TARGETING_CONDITIONS = TargetingConditions.forCombat().range(20.0).selector(LIVING_ENTITY_SELECTOR);
+    private java.util.@Nullable UUID summoner; // Pempek - Summoner API
 
     public WitherBoss(final EntityType<? extends WitherBoss> type, final Level level) {
         super(type, level);
@@ -89,6 +_,16 @@
         this.xpReward = 50;
     }
 
+    // Pempek start - Summoner API
+    public java.util.@Nullable UUID getSummoner() {
+        return summoner;
+    }
+
+    public void setSummoner(java.util.@Nullable UUID summoner) {
+        this.summoner = summoner;
+    }
+    // Pempek end - Summoner API
+
     @Override
     protected PathNavigation createNavigation(final Level level) {
         FlyingPathNavigation flyingPathNavigation = new FlyingPathNavigation(this, level);
@@ -121,6 +_,7 @@
     protected void addAdditionalSaveData(final ValueOutput output) {
         super.addAdditionalSaveData(output);
         output.putInt("Invul", this.getInvulnerableTicks());
+        output.storeNullable("Pempek.Summoner", net.minecraft.core.UUIDUtil.CODEC, getSummoner()); // Pempek - Summoner API
     }
 
     @Override
@@ -130,6 +_,7 @@
         if (this.hasCustomName()) {
             this.bossEvent.setName(this.getDisplayName());
         }
+        this.setSummoner(input.read("Pempek.Summoner", net.minecraft.core.UUIDUtil.CODEC).orElse(null)); // Pempek - Summoner API
     }
 
     @Override
@@ -273,7 +_,7 @@
                     level.explode(this, this.getX(), this.getEyeY(), this.getZ(), event.getRadius(), event.getFire(), Level.ExplosionInteraction.MOB);
                 }
                 // CraftBukkit end
-                if (!this.isSilent()) {
+                if (!this.isSilent() && level.pempekConfig.witherPlaySpawnSound) { // Pempek - Toggle for Wither's spawn sound
                     // CraftBukkit start - Use relative location for far away sounds
                     // level.globalLevelEvent(LevelEvent.SOUND_WITHER_BOSS_SPAWN, this.blockPosition(), 0);
                     int viewDistance = level.getCraftServer().getViewDistance() * 16;
@@ -377,8 +_,10 @@
                 }
             }
 
-            if (this.tickCount % 20 == 0) {
-                this.heal(1.0F, org.bukkit.event.entity.EntityRegainHealthEvent.RegainReason.REGEN); // CraftBukkit
+            // Pempek start - Customizable wither health and healing - customizable heal rate and amount
+            if (this.tickCount % level().pempekConfig.witherHealthRegenDelay == 0) {
+                this.heal(level().pempekConfig.witherHealthRegenAmount, org.bukkit.event.entity.EntityRegainHealthEvent.RegainReason.REGEN); // CraftBukkit
+            // Pempek end - Customizable wither health and healing
             }
 
             this.bossEvent.setProgress(this.getHealth() / this.getMaxHealth());
@@ -584,6 +_,7 @@
 
     @Override
     protected boolean canRide(final Entity vehicle) {
+        if (this.level().pempekConfig.witherCanRideVehicles) return this.boardingCooldown <= 0; // Pempek - Configs for if Wither/Ender Dragon can ride vehicles
         return false;
     }
 
