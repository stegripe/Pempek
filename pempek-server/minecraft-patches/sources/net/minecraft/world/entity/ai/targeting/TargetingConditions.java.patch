--- a/net/minecraft/world/entity/ai/targeting/TargetingConditions.java
+++ b/net/minecraft/world/entity/ai/targeting/TargetingConditions.java
@@ -67,6 +_,10 @@
 
         if (this.selector != null && !this.selector.test(target, level)) {
             return false;
+        // Pempek start - AFK API
+        } else if (!level.pempekConfig.idleTimeoutTargetPlayer && target instanceof net.minecraft.server.level.ServerPlayer player && player.isAfk()) {
+            return false;
+        // Pempek end - AFK API
         }
 
         if (targeter == null) {
