--- a/net/minecraft/world/entity/monster/zombie/ZombieVillager.java
+++ b/net/minecraft/world/entity/monster/zombie/ZombieVillager.java
@@ -166,10 +_,10 @@
     public InteractionResult mobInteract(final Player player, final InteractionHand hand) {
         ItemStack itemStack = player.getItemInHand(hand);
         if (itemStack.is(Items.GOLDEN_APPLE)) {
-            if (this.hasEffect(MobEffects.WEAKNESS)) {
+            if (this.hasEffect(MobEffects.WEAKNESS) && level().pempekConfig.zombieVillagerCureEnabled) { // Pempek - Add option to disable zombie villagers cure
                 itemStack.consume(1, player);
                 if (!this.level().isClientSide()) {
-                    this.startConverting(player.getUUID(), this.random.nextInt(2401) + 3600);
+                    this.startConverting(player.getUUID(), this.random.nextInt(level().pempekConfig.zombieVillagerCuringTimeMax - level().pempekConfig.zombieVillagerCuringTimeMin + 1) + level().pempekConfig.zombieVillagerCuringTimeMin); // Pempek - Customizable Zombie Villager curing times
                 }
 
                 return InteractionResult.SUCCESS_SERVER;
