--- a/net/minecraft/world/entity/player/Player.java
+++ b/net/minecraft/world/entity/player/Player.java
@@ -174,11 +_,20 @@
     public float hurtDir;
     public boolean affectsSpawning = true; // Paper - Affects Spawning API
     public net.kyori.adventure.util.TriState flyingFallDamage = net.kyori.adventure.util.TriState.NOT_SET; // Paper - flying fall damage
+    public int burpDelay = 0; // Pempek - Burp delay
+    public boolean canPortalInstant = false; // Pempek - Add portal permission bypass
 
     // CraftBukkit start
     public boolean fauxSleeping;
     public int oldLevel = -1;
 
+    // Pempek start - AFK API
+    public abstract void setAfk(boolean afk);
+
+    public boolean isAfk() {
+        return false;
+    }
+    // Pempek end - AFK API
     @Override
     public org.bukkit.craftbukkit.entity.CraftHumanEntity getBukkitEntity() {
         return (org.bukkit.craftbukkit.entity.CraftHumanEntity) super.getBukkitEntity();
@@ -243,6 +_,12 @@
 
     @Override
     public void tick() {
+        // Pempek start - Burp delay
+        if (this.burpDelay > 0 && --this.burpDelay == 0) {
+            this.level().playSound(null, getX(), getY(), getZ(), SoundEvents.PLAYER_BURP, SoundSource.PLAYERS, 1.0F, this.level().getRandom().nextFloat() * 0.1F + 0.9F);
+        }
+        // Pempek end - Burp delay
+
         this.noPhysics = this.isSpectator();
         if (this.isSpectator() || this.isPassenger()) {
             this.setOnGround(false);
@@ -300,6 +_,17 @@
             this.turtleHelmetTick();
         }
 
+        // Pempek start - Full netherite armor grants fire resistance
+        if (this.level().pempekConfig.playerNetheriteFireResistanceDuration > 0 && this.level().getGameTime() % 20 == 0) {
+            if (this.getItemBySlot(EquipmentSlot.HEAD).is(Items.NETHERITE_HELMET)
+                && this.getItemBySlot(EquipmentSlot.CHEST).is(Items.NETHERITE_CHESTPLATE)
+                && this.getItemBySlot(EquipmentSlot.LEGS).is(Items.NETHERITE_LEGGINGS)
+                && this.getItemBySlot(EquipmentSlot.FEET).is(Items.NETHERITE_BOOTS)) {
+                this.addEffect(new MobEffectInstance(MobEffects.FIRE_RESISTANCE, this.level().pempekConfig.playerNetheriteFireResistanceDuration, this.level().pempekConfig.playerNetheriteFireResistanceAmplifier, this.level().pempekConfig.playerNetheriteFireResistanceAmbient, this.level().pempekConfig.playerNetheriteFireResistanceShowParticles, this.level().pempekConfig.playerNetheriteFireResistanceShowIcon), org.bukkit.event.entity.EntityPotionEffectEvent.Cause.NETHERITE_ARMOR);
+            }
+        }
+        // Pempek end - Full netherite armor grants fire resistance
+
         this.cooldowns.tick();
         this.updatePlayerPose();
     }
@@ -505,7 +_,7 @@
             List<Entity> orbs = Lists.newArrayList();
 
             for (Entity entity : entities) {
-                if (entity.is(EntityTypes.EXPERIENCE_ORB)) {
+                if (entity.is(EntityTypes.EXPERIENCE_ORB) && entity.level().pempekConfig.playerExpPickupDelay >= 0) { // Pempek - Configurable player pickup exp delay
                     orbs.add(entity);
                 } else if (!entity.isRemoved()) {
                     this.touch(entity);
@@ -1053,7 +_,7 @@
                     criticalAttack = criticalAttack && !this.level().paperConfig().entities.behavior.disablePlayerCrits; // Paper - Toggleable player crits
                     if (criticalAttack) {
                         damageSource = damageSource.critical(); // Paper - critical damage API
-                        baseDamage *= 1.5F;
+                        baseDamage *= (float) this.level().pempekConfig.playerCriticalDamageMultiplier; // Pempek - Add config change multiplier critical damage value
                     }
 
                     float totalDamage = baseDamage + magicBoost;
@@ -1763,7 +_,23 @@
 
     @Override
     protected int getBaseExperienceReward(final ServerLevel level) {
-        return !level.getGameRules().get(GameRules.KEEP_INVENTORY) && !this.isSpectator() ? Math.min(this.experienceLevel * 7, 100) : 0;
+        // Pempek start - Add player death exp control options
+        if (!level.getGameRules().get(GameRules.KEEP_INVENTORY) && !this.isSpectator()) {
+            int toDrop;
+            try {
+                toDrop = Math.round(((Number) scriptEngine.eval("let expLevel = " + experienceLevel + "; " +
+                    "let expTotal = " + totalExperience + "; " +
+                    "let exp = " + experienceProgress + "; " +
+                    level().pempekConfig.playerDeathExpDropEquation)).floatValue());
+            } catch (javax.script.ScriptException e) {
+                e.printStackTrace();
+                toDrop = experienceLevel * 7;
+            }
+            return Math.min(toDrop, level().pempekConfig.playerDeathExpDropMax);
+        } else {
+            return 0;
+        }
+        // Pempek end - Add player death exp control options
     }
 
     @Override
@@ -1806,6 +_,13 @@
     public boolean addItem(final ItemStack itemStack) {
         return this.inventory.add(itemStack);
     }
+
+    // Pempek start - Player ridable in water option
+    @Override
+    public boolean dismountsUnderwater() {
+        return !level().pempekConfig.playerRidableInWater;
+    }
+    // Pempek end - Player ridable in water option
 
     public abstract @Nullable GameType gameMode();
 
