--- a/net/minecraft/world/entity/animal/equine/Llama.java
+++ b/net/minecraft/world/entity/animal/equine/Llama.java
@@ -76,6 +_,7 @@
     private boolean didSpit;
     private @Nullable Llama caravanHead;
     public @Nullable Llama caravanTail; // Paper - public
+    public boolean shouldJoinCaravan = true; // Pempek - Llama API
 
     public Llama(final EntityType<? extends Llama> type, final Level level) {
         super(type, level);
@@ -105,6 +_,7 @@
         super.addAdditionalSaveData(output);
         output.store("Variant", Llama.Variant.LEGACY_CODEC, this.getVariant());
         output.putInt("Strength", this.getStrength());
+        output.putBoolean("Pempek.ShouldJoinCaravan", shouldJoinCaravan); // Pempek - Llama API
     }
 
     @Override
@@ -112,6 +_,7 @@
         this.setStrength(input.getIntOr("Strength", 0));
         super.readAdditionalSaveData(input);
         this.setVariant(input.read("Variant", Llama.Variant.LEGACY_CODEC).orElse(Llama.Variant.DEFAULT));
+        this.shouldJoinCaravan = input.getBooleanOr("Pempek.ShouldJoinCaravan", true); // Pempek - Llama API
     }
 
     @Override
@@ -388,6 +_,7 @@
 
     public void leaveCaravan() {
         if (this.caravanHead != null) {
+            new org.stegripe.pempek.event.entity.LlamaLeaveCaravanEvent((org.bukkit.entity.Llama) getBukkitEntity()).callEvent(); // Pempek - Llama API
             this.caravanHead.caravanTail = null;
         }
 
@@ -395,6 +_,7 @@
     }
 
     public void joinCaravan(final Llama tail) {
+        if (!this.level().pempekConfig.llamaJoinCaravans || !shouldJoinCaravan || !new org.stegripe.pempek.event.entity.LlamaJoinCaravanEvent((org.bukkit.entity.Llama) getBukkitEntity(), (org.bukkit.entity.Llama) tail.getBukkitEntity()).callEvent()) return; // Pempek - Llama API // Pempek - Config to disable Llama caravans
         this.caravanHead = tail;
         this.caravanHead.caravanTail = this;
     }
