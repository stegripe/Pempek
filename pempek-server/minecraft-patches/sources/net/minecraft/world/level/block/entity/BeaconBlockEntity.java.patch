--- a/net/minecraft/world/level/block/entity/BeaconBlockEntity.java
+++ b/net/minecraft/world/level/block/entity/BeaconBlockEntity.java
@@ -140,6 +_,16 @@
 
     public double getEffectRange() {
         if (this.effectRange < 0) {
+            // Pempek start - Beacon Activation Range Configurable
+            if (this.level != null) {
+                switch (this.levels) {
+                    case 1: return this.level.pempekConfig.beaconLevelOne;
+                    case 2: return this.level.pempekConfig.beaconLevelTwo;
+                    case 3: return this.level.pempekConfig.beaconLevelThree;
+                    case 4: return this.level.pempekConfig.beaconLevelFour;
+                }
+            }
+            // Pempek end - Beacon Activation Range Configurable
             return this.levels * 10 + 10;
         } else {
             return effectRange;
@@ -168,6 +_,7 @@
         int y = pos.getY();
         int z = pos.getZ();
         BlockPos checkPos;
+        boolean isTintedGlass = false; // Pempek - allow beacon effects when covered by tinted glass
         if (entity.lastCheckY < y) {
             checkPos = pos;
             entity.checkingBeamSections = Lists.newArrayList();
@@ -197,11 +_,15 @@
                     }
                 }
             } else {
-                if (lastBeamSection == null || state.getLightDampening() >= 15 && !state.is(Blocks.BEDROCK)) {
+                if (level.pempekConfig.beaconAllowEffectsWithTintedGlass && state.getBlock().equals(Blocks.TINTED_GLASS)) {isTintedGlass = true;} // Pempek - allow beacon effects when covered by tinted glass
+                // Pempek start - fix effects being applied when tinted glass is covered
+                if (lastBeamSection == null || state.getLightDampening() >= 15 && !state.is(Blocks.BEDROCK) && !(state.getBlock().equals(Blocks.TINTED_GLASS) && level.pempekConfig.beaconAllowEffectsWithTintedGlass)) {
                     entity.checkingBeamSections.clear();
                     entity.lastCheckY = lastSetBlock;
+                    isTintedGlass = false;
                     break;
                 }
+                // Pempek end - fix effects being applied when tinted glass is covered
 
                 lastBeamSection.increaseHeight();
             }
@@ -212,11 +_,11 @@
 
         int previousLevels = entity.levels;
         if (level.getGameTime() % 80L == 0L) {
-            if (!entity.beamSections.isEmpty()) {
+            if (!entity.beamSections.isEmpty() || (level.pempekConfig.beaconAllowEffectsWithTintedGlass && isTintedGlass)) { // Pempek - fix beacon effects persisting with broken base while tinted glass is used
                 entity.levels = updateBase(level, x, y, z);
             }
 
-            if (entity.levels > 0 && !entity.beamSections.isEmpty()) {
+            if (entity.levels > 0 && (!entity.beamSections.isEmpty() || (level.pempekConfig.beaconAllowEffectsWithTintedGlass && isTintedGlass))) { // Pempek - allow beacon effects when covered by tinted glass
                 applyEffects(level, pos, entity.levels, entity.primaryPower, entity.secondaryPower, entity); // Paper - Custom beacon ranges
                 playSound(level, pos, SoundEvents.BEACON_AMBIENT);
             }
