--- a/net/minecraft/world/level/block/ObserverBlock.java
+++ b/net/minecraft/world/level/block/ObserverBlock.java
@@ -82,6 +_,7 @@
         final RandomSource random
     ) {
         if (state.getValue(FACING) == directionToNeighbour && !state.getValue(POWERED)) {
+            if (!(level.getWorldBorder().world != null && level.getWorldBorder().world.pempekConfig.disableObserverClocks) || !(neighbourState.getBlock() instanceof ObserverBlock) || neighbourState.getValue(ObserverBlock.FACING).getOpposite() != directionToNeighbour) // Pempek - Add Option for disable observer clocks
             this.startSignal(level, ticks, pos);
         }
 
