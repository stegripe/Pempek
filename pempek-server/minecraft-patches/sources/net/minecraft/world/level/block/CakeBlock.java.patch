--- a/net/minecraft/world/level/block/CakeBlock.java
+++ b/net/minecraft/world/level/block/CakeBlock.java
@@ -119,6 +_,7 @@
         org.bukkit.event.entity.FoodLevelChangeEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callFoodLevelChangeEvent(player, 2 + oldFoodLevel);
 
         if (!event.isCancelled()) {
+            if (player.level().pempekConfig.playerBurpWhenFull && event.getFoodLevel() == 20 && oldFoodLevel < 20) player.burpDelay = player.level().pempekConfig.playerBurpDelay; // Pempek - Burp after eating food fills hunger bar completely
             player.getFoodData().eat(event.getFoodLevel() - oldFoodLevel, 0.1F);
         }
 
