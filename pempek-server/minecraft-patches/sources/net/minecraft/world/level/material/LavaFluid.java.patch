--- a/net/minecraft/world/level/material/LavaFluid.java
+++ b/net/minecraft/world/level/material/LavaFluid.java
@@ -191,7 +_,7 @@
 
     @Override
     public int getTickDelay(final LevelReader level) {
-        return isFastLava(level) ? 10 : 30;
+        return level.getWorldBorder().world != null ? (isFastLava(level) ? level.getWorldBorder().world.pempekConfig.lavaSpeedNether : level.getWorldBorder().world.pempekConfig.lavaSpeedNotNether) : (isFastLava(level) ? 10 : 30); // Pempek - Make lava flow speed configurable
     }
 
     @Override
@@ -212,6 +_,13 @@
     private void fizz(final LevelAccessor level, final BlockPos pos) {
         level.levelEvent(LevelEvent.LAVA_FIZZ, pos, 0);
     }
+
+    // Pempek start - Implement infinite liquids
+    @Override
+    protected int getRequiredSources(Level level) {
+        return level.pempekConfig.lavaInfiniteRequiredSources;
+    }
+    // Pempek end - Implement infinite liquids
 
     @Override
     protected boolean canConvertToSource(final ServerLevel level) {
